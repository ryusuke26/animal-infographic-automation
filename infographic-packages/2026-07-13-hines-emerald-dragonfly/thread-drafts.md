# Thread Drafts

The locked Japanese and English main posts fit the intended standalone X-post limit, so no thread is required. If context is split into a reply, use only the locked source/context reply already provided in each combined posting set.
